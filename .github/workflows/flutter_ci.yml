name: Flutter CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: cirrusci/flutter:latest

    steps:
      - uses: actions/checkout@v2

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: dev
          flutter-version: 3.5.0-122.0.dev
      - run: flutter --version

      - name: Get dependencies
        run: flutter pub get

      - name: Run Melos bootstrap
        run: melos bootstrap

      - name: Run tests
        run: melos run test

      - name: Run lint
        run: melos run lint

      - name: Analyze
        run: melos run analyze

      - name: Build
        run: melos run build_runner